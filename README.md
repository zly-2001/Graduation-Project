# Graduation-Project
## zly纯原创毕业设计终于完成啦！嘻嘻😁
## 水印训练python代码不包含实验结果图但是你可以自己找coco或者其他数据集就可以哦
Mac系统可以直接跑不过有些不支持MPS，所以支持的用MPS，不支持的回退用CPU
所以在在命令行加echo 'export PYTORCH_ENABLE_MPS_FALLBACK=1' >> ~/.zshrc 然后source ~/.zshrc
## 仓库后期还会再加一个模型集成到java的springboot水印溯源系统